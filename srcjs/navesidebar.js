import {addShowOnlyTabpanelEvent} from './utils.js';

$( document ).ready(function() {
    addShowOnlyTabpanelEvent(
      menu_class = "fr-sidemenu__link",
      tabpanel_class = "fr-tabs__panel_navsidebarpanel");
});
